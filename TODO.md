**Remaining**
- Backfill `tenantId` on existing data.

**Open Questions**
- Decide whether `tenantId` should be derived from auth token claims or a new tenant table.
- Confirm desired semantics for tag `count` when removing tags from assets.
